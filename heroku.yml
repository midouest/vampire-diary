setup:
  addons:
    - plan: heroku-postgresql

build:
  docker:
    web: Dockerfile

release:
  image: web
  command:
    - cd web && yarn build
    - python manage.py collectstatic --noinput

run:
  web: gunicorn config.wsgi
